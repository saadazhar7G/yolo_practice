[project]
name = "yolo-poetry"
version = "0.1.0"
description = ""
authors = [
    {name = "saadazhar7G",email = "saad.azhar@granlund.fi"}
]
readme = "README.md"
requires-python = ">=3.9.0"
dependencies = [
]

[tool.poetry]
packages = [{include = "yolo_poetry", from = "src"}]

[build-system]
requires = ["poetry-core>=2.0.0,<3.0.0"]
build-backend = "poetry.core.masonry.api"
